# Dashboard Clarity & Screenshots (Fase 1.5) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the dashboard self-explanatory and screenshot-clean — label every card's basis, add a metric glossary + a reconciliation note, and replace the checkbox toggles with a clickable legend.

**Architecture:** All changes live in `tools/build-report.ts` (the HTML template, its `<style>`, and the inline `<script>`). No new columns, no scanner changes, no re-scan. `report.html` is regenerated by the daily workflow.

**Tech Stack:** TypeScript (ESM), `tsx`, Chart.js 4.4.1 (CDN), vanilla DOM JS. Windows + Git Bash for git.

## Global Constraints

- Report-only: edit ONLY `tools/build-report.ts`. No `history.ts`/`scan.ts`/`metrics.ts`, no CSV columns, no re-scan.
- The 31 existing vitest tests must stay green (`npm test`).
- NEVER commit the subfrost key — run `git diff | grep 3cdaa58a` before each commit; expect no match.
- `build-report.ts` is a generator with no unit test; verify by running it + grepping `report.html` + `node --check` on the extracted inline script + visual render.
- Theme vars already defined: `--teal #2DBE8E`, `--purple #9d94e8`, `--amber #E9A23B`, `--faint #6f6f78`, `--muted #9a9aa3`, `--head`. Daily chart `#g` dataset order: 0 OP_RETURN bytes, 1 Transactions, 2 OP_RETURN penetration, 3 Runes, 4 Alkanes excl. DIESEL. The chart is captured as `const gChart`.
- Commit messages in Portuguese; end each with `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

---

## File Structure

- `tools/build-report.ts` — only file touched. Changes: `.card .u` CSS + clickable-legend CSS; `card()` helper copy; penetration card copy; glossary + reconciliation note in the HTML; legend spans get `data-ds`; checkbox row + bind script replaced by a legend-click handler.

---

### Task 1: Card labels + glossary + reconciliation note (static HTML)

**Files:**
- Modify: `tools/build-report.ts` — `.card` CSS block (~line 68); `card()` helper (~line 34-35); penetration card (in cards block); "How it's calculated" `.how` block (~line 100-105); add a reconciliation `.note`.

**Interfaces:**
- Consumes: existing `r1`, `alkShareCount`, `alkBytesShare`, `opReturnShare`, `all`, `d30` (already in file).
- Produces: clearer card copy + a `.card .u` sub-label class; a glossary `<p>`; a reconciliation `<p class="note">`. No JS API changes.

- [ ] **Step 1: Add the `.u` sub-label CSS**

In the `.card` CSS line (~68), append a rule for `.u`. Change:

```
  .card .l{font-size:13px;color:var(--muted)} .card .v{font-size:27px;font-weight:600;color:var(--head);margin-top:2px} .card .b{font-size:12px;color:var(--faint);margin-top:2px}
```
to:
```
  .card .l{font-size:13px;color:var(--muted)} .card .v{font-size:27px;font-weight:600;color:var(--head);margin-top:2px} .card .u{font-size:11px;color:var(--muted);margin-top:1px} .card .b{font-size:12px;color:var(--faint);margin-top:2px}
```

- [ ] **Step 2: Relabel the `card()` helper**

Change the `card` helper (~line 34-35) from:

```ts
const card = (label: string, s: Sums) =>
  `<div class="card"><div class="l">${label}</div><div class="v">${r1(alkShareCount(s))}%</div><div class="b">${r1(alkBytesShare(s))}% of bytes</div></div>`;
```
to:
```ts
const card = (label: string, s: Sums) =>
  `<div class="card"><div class="l">${label}</div><div class="v">${r1(alkShareCount(s))}%</div><div class="u">Alkanes — of all BTC tx</div><div class="b">${r1(alkBytesShare(s))}% of OP_RETURN bytes</div></div>`;
```

- [ ] **Step 3: Relabel the penetration card**

Change the penetration card line (in the cards block) from:

```html
  <div class="card"><div class="l">OP_RETURN penetration</div><div class="v">${r1(opReturnShare(all))}%</div><div class="b">${r1(opReturnShare(d30))}% last 30 days</div></div>
```
to:
```html
  <div class="card"><div class="l">OP_RETURN penetration</div><div class="v">${r1(opReturnShare(all))}%</div><div class="u">of all BTC tx (carry OP_RETURN)</div><div class="b">${r1(opReturnShare(d30))}% last 30 days</div></div>
```

- [ ] **Step 4: Add the reconciliation note**

Immediately after the closing `</div>` of the `<div class="cards"> ... </div>` block, add:

```html
<p class="note">Numbers you may see elsewhere can differ — they depend on the time window and whether they measure transactions vs bytes vs outputs (and whether the coinbase is included).</p>
```

- [ ] **Step 5: Add the glossary to "How it's calculated"**

Inside the `<div class="how"> ... </div>` block, add this paragraph as the last child (before `</div>`):

```html
  <p><b>Glossary.</b> <b>OP_RETURN penetration</b>: share of all BTC tx that carry an OP_RETURN. <b>Alkanes (tx)</b>: share of all BTC tx that are Alkanes. <b>Alkanes (bytes)</b>: share of OP_RETURN bytes that are Alkanes. <b>Runes</b>: OP_RETURN bytes that are Runestones but not Alkanes. <b>Alkanes excl. DIESEL</b>: Alkanes tx that aren't DIESEL mints — "real app" usage. <b>DIESEL</b>: mint of the genesis alkane (cellpack <code>2:0</code> op&nbsp;77).</p>
```

- [ ] **Step 6: Regenerate and verify**

```bash
cd /c/OpDecoder/opreturn-scanner
./node_modules/.bin/tsx tools/build-report.ts
grep -c 'of all BTC tx' report.html        # expect >=4 (4 alkanes cards + penetration card uses different copy) -> at least 4
grep -c 'of OP_RETURN bytes' report.html    # expect >=4 (4 alkanes cards)
grep -c 'Glossary.' report.html             # expect 1
grep -c 'Numbers you may see elsewhere' report.html  # expect 1
```
Expected: `of all BTC tx` ≥ 4; `of OP_RETURN bytes` ≥ 4; glossary 1; note 1.

- [ ] **Step 7: Commit**

```bash
git diff | grep 3cdaa58a || echo "no key"   # must print "no key"
git add tools/build-report.ts report.html
git commit -m "feat: rótulos claros nos cards + glossário + nota de reconciliação" -m "Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: Clickable legend (replaces checkbox toggles)

**Files:**
- Modify: `tools/build-report.ts` — `.legend` CSS (~line 70); daily-chart legend spans (add `data-ds`); remove the checkbox `<div class="legend">` row; replace the checkbox bind script with a legend-click handler.

**Interfaces:**
- Consumes: `gChart` (the `const gChart=new Chart(g,{...})` from Fase 1); the daily legend `<span>` items.
- Produces: clickable legend (each `span[data-ds]` toggles its dataset; `.off` class dims+strikes it). No checkbox elements (`tgPen`/`tgRunes`/`tgAlkEx`) remain.

- [ ] **Step 1: Add clickable-legend CSS**

In the `.legend` CSS line (~70), append two rules. After:

```
  .legend{display:flex;flex-wrap:wrap;gap:16px;margin:8px 0 10px;font-size:12px;color:var(--muted);align-items:center}
```
add:
```
  .legend span[data-ds]{cursor:pointer;user-select:none} .legend span.off{opacity:.4;text-decoration:line-through}
```

- [ ] **Step 2: Add `data-ds` to the daily legend spans**

Replace the daily-chart legend line (the one with OP_RETURN bytes / Transactions / penetration / Runes / Alkanes excl. DIESEL) with the same content but each outer `<span>` carrying its dataset index:

```html
<div class="legend"><span data-ds="0"><span class="sw" style="background:var(--teal)"></span>OP_RETURN bytes</span><span data-ds="1"><span class="sw" style="background:var(--purple)"></span>Transactions</span><span data-ds="2"><span class="sw" style="background:var(--faint)"></span>OP_RETURN penetration</span><span data-ds="3"><span class="sw" style="background:var(--amber)"></span>Runes (bytes)</span><span data-ds="4"><span class="sw" style="background:#4bb8d9"></span>Alkanes excl. DIESEL (tx)</span></div>
```

- [ ] **Step 3: Remove the checkbox row**

Delete the entire checkbox block that follows that legend:

```html
<div class="legend" style="margin-bottom:6px">
  <label><input type="checkbox" id="tgPen" checked> OP_RETURN penetration</label>
  <label><input type="checkbox" id="tgRunes" checked> Runes</label>
  <label><input type="checkbox" id="tgAlkEx" checked> Alkanes excl. DIESEL</label>
</div>
```

- [ ] **Step 4: Replace the bind script with a legend-click handler**

Replace the checkbox bind line:

```js
[['tgPen',2],['tgRunes',3],['tgAlkEx',4]].forEach(([id,idx])=>{const el=document.getElementById(id);el.addEventListener('change',()=>{gChart.setDatasetVisibility(idx,el.checked);gChart.update();});});
```
with:
```js
document.querySelectorAll('.legend span[data-ds]').forEach(el=>{el.addEventListener('click',()=>{const i=+el.dataset.ds;const vis=gChart.isDatasetVisible(i);gChart.setDatasetVisibility(i,!vis);el.classList.toggle('off',vis);gChart.update();});});
```

- [ ] **Step 5: Regenerate and verify (incl. JS syntax)**

```bash
cd /c/OpDecoder/opreturn-scanner
./node_modules/.bin/tsx tools/build-report.ts
grep -c 'data-ds=' report.html               # expect 5 (the daily legend spans)
grep -c 'tgRunes' report.html                # expect 0 (checkboxes gone)
grep -c 'isDatasetVisible' report.html        # expect 1
node -e 'const fs=require("fs");const h=fs.readFileSync("report.html","utf8");const m=[...h.matchAll(/<script>([\s\S]*?)<\/script>/g)];fs.writeFileSync("report-inline.tmp.js",m[m.length-1][1]);' && node --check report-inline.tmp.js && echo "OK: inline JS valido"; rm -f report-inline.tmp.js
```
Expected: `data-ds=` 5; `tgRunes` 0; `isDatasetVisible` 1; "OK: inline JS valido".
Manual render: open `report.html`, confirm clicking a legend item (e.g. "Runes (bytes)") hides its line and dims/strikes the label; clicking again restores it.

- [ ] **Step 6: Commit**

```bash
git diff | grep 3cdaa58a || echo "no key"   # must print "no key"
git add tools/build-report.ts report.html
git commit -m "feat: legenda clicável no gráfico diário (remove checkboxes, print limpo)" -m "Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Final verification (after both tasks)

- [ ] `npm test` → **31 passing** (unchanged).
- [ ] `./node_modules/.bin/tsx tools/build-report.ts` runs clean; cards labeled, glossary + note present, legend clickable, no checkbox row.
- [ ] `git push origin master`; publish via `gh workflow run daily.yml --repo Vdto88/alkanes-opreturn-scanner --ref master`, then verify the live site (cache-busted) shows the new labels + clickable legend.

## Out of scope (Fase 2, separate plan + re-scan)

Fees/miners (USD+BTC) and breaking the non-DIESEL slice into transfers/swaps/deploys. See `docs/superpowers/specs/2026-06-24-dashboard-metrics-expansion-design.md`.
